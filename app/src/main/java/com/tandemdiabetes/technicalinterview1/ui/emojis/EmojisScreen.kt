package com.tandemdiabetes.technicalinterview1.ui.emojis

import androidx.compose.runtime.Composable

@Composable
fun EmojisScreen() {
    TODO("Implement the UI")
    // 1 - The screen should display a Centered progress bar while the application if fetching the data
    // 2 - When the data is retrieved, we should see a list of Emojis. Each item should have the emoji itself and the text/name of the emoji
}
